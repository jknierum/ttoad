----------TO DO-----------
CURRENT-------------------
- Find typed word

- when selecting keep cursor in place and revert select by re keying

f key=lastkey:
    select_on = false (needs to be a new var not last key because of -1)

- woring on listing key binds under -h
! undo and redo should store scroll pos

MAJOR---------------------
- redesign  actions to be built from parts
    - selection (sel,block,word, etc)
    - repeats (number)
    - Action (delete, copy, jump)

    b12d = delete the next 12 blocks
    b-12d = delete the last 12 blocks
- find

MINOR---------------------
- home should bring to the frount of the text in line
- auto both (), "", {}. [], ''
- cant scroll to the edge of the doc (right)




--ACTIONS--
# (todo)
    modified actions (ctrl + ..) used for file management
        save: ctrl + s
#        write to new: ctrl + w
        quit: ctrl + q
#        copy: ctrl + c  (needs sel)
        paste: ctrl + p
        undo: ctrl + u
        redo: ctrl + y
        move Tinty to back: ctrl + z (fg to bring back to front)

    action types: used for text manipulation
        locators
            l(line)
            b(block)
            w(word)
#            a(all) - not yet added

        actions
            delete(d)
            select(s)
            copy(c)/yank(y)
            paste(p)
            paste over line(P)
            cut(x)
            jump forword(j)
            jump back(J)
            comment(/)
            tab(t)
            unTab(T)
            find sel word(f)
Extras:
    auto saves
    have actions that stack


----------DONE------------
- block sel fixed ignores # when selecting
- when find word cursor should jump to location
- reintroduceed 50ms updatng so find text would not wait for next key press
- added find typed text
- tab / btab to cycle through find options

--------PREVIOUS----------
- when backspace to prev line the cursor jumps to end of line
- tab select
- comment select
- delete select
- removed update every 50ms to save cpu
- find sel word(f)
- fixed it so auto complete only aqpears when typing
- crashing when autoComp is out of bounds
- added non case sensitive autoComp
- selecting
- half scrolling for pgup/pgdn
- fixed page up and down







