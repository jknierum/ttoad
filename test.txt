----------TO DO-----------
CURRENT-------------------
! block sel bug (selecting lower than it above)
- if selecting and repeats sel key: undo
- working on listing key binds under -h

MAJOR---------------------

MINOR---------------------
- auto both (), "", {}. [], ''
- cant scroll to the edge of the doc (right)




--ACTIONS--
# (todo)
    modified actions (ctrl + ..) used for file management
        save: ctrl + s
#        write to new: ctrl + w
        quit: ctrl + q
        paste: ctrl + p
        copy: ctrl + y
        undo: ctrl + u
        redo: ctrl + r
        move Tinty to back: ctrl + z (fg to bring back to front)

    action types: used for text manipulation
        locators
            l(line)
            b(block)
            w(word)
#            a(all) - not yet added

        actions
            delete(d)
            select(s)
            copy(c)/yank(y)
            paste(p)
            paste over line(P)
            cut(x)
            jump forword(j)
            jump back(J)
            comment(/)
            tab(t)
            unTab(T)
            find sel word(f)
Extras:
    auto saves
    have actions that stack


----------DONE------------


--------PREVIOUS----------
- home brings to the front of the text in line
! up and down now exits find when not autocomplete list
! undo and redo should store scroll pos
! paste crash bug
- any of the ctrl actions should pull you from find
- ctrl + y = yank
- cut (x, ctrl + x)
- paste now no longer adds a /n after the cursor
- find live highlights and jumps top next word
- find has autocomplete that inserts with enter
- autocomplete inserts with enter
- can scroll through autocomplete when in find now
- block sel fixed ignores # when selecting
- when find word cursor should jump to location
- reintroduceed 50ms updatng so find text would not wait for next key press
- added find typed text
- tab / btab to cycle through find options
- when backspace to prev line the cursor jumps to end of line
- tab select
- comment select
- delete select
- removed update every 50ms to save cpu
- find sel word(f)
- fixed it so auto complete only aqpears when typing
- crashing when autoComp is out of bounds
- added non case sensitive autoComp
- selecting
- half scrolling for pgup/pgdn
- fixed page up and down







